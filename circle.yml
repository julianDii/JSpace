general:
  build_dir: Jspace

machine:
  node:
    version: 7

dependencies:
  pre:
    - npm install npm@latest -g # Should fix bug in ng test
    - npm install angular/cli@latest -g

test:  
  override:
    # Circle by default will run npm test command,sdasdsad
    # We want to override it so we can generate coverage as-well.
    - ng e2e
    - cd JSpace && cd Jspace && ng test
